<Window x:Class="WPFGameShop.GameListWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WPFGameShop" xmlns:wpfcontrollibrary="clr-namespace:WpfControlLibrary;assembly=UserControls"
		mc:Ignorable="d"
        Title="GameListWindow" Height="450" Width="800">
	<Window.DataContext>
		<Binding RelativeSource="{RelativeSource Self}"/>
	</Window.DataContext>
    <Grid>

		<ListBox x:Name="List" ItemsSource="{Binding Path =GameListViewModel.GameModelList}"
				  SelectedItem="{Binding Path= GameListViewModel.SelectedItem}" Background="Transparent"
				 ItemTemplate="{StaticResource ListBoxItem}">

		 



		</ListBox>

	</Grid>
</Window>
